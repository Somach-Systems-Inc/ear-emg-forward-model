# Paper 1 -- ear-emg-forward-model
#
# Python 3.11 to match the interpreter SimNIBS 4.6.0 is built for (cp311),
# so this venv and the SimNIBS environment stay on the same footing.
#
#   uv venv --python 3.11 .venv
#   source .venv/bin/activate
#   uv pip install -r requirements.txt
#
# Versions pinned for reproducibility of the published figures.

numpy==2.4.6
scipy==1.17.1
nibabel==5.4.2
matplotlib==3.11.1
pandas==3.0.5

# SimNIBS is deliberately absent from this file and must NOT be added to it.
#
# Do not pip-install the SimNIBS wheel into this venv. It resolves only after
# you supply six non-PyPI dependencies by URL, and then still fails: the CGAL
# extensions carry an rpath to the maintainer's build machine, and the import
# chain requires `mumps`, which is sdist-only and has no Homebrew formula.
# README.md records the full autopsy.
#
# Install SimNIBS 4.6.0 from simnibs_installer_macos.pkg instead. It lives
# outside this venv with its own Python, and 01_build_mesh.py finds `meshmesh`
# on PATH via shutil.which().
#
# Only the mesh build needs it. `--list-labels` runs on numpy + nibabel alone,
# which is why the MIDA label question was settled before SimNIBS existed here.
